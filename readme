
Tutorial: https://www.digitalocean.com/community/tutorials/how-to-install-wordpress-with-docker-compose

Clone This Repo (fill in PASSWORD):
"""
sudo apt-get update &&
sudo apt-get upgrade -y &&
sudo apt-get install git -y &&
sudo git clone https://briefchase:PASSWORD@github.com/briefchase/docker-wordpress.git &&
cd docker-wordpress
"""

Use:
 - Modify make file as desired
 - make [setup go service stop]

Check status:
 - docker-compose ps
 If service is not 0 or up:
   - docker-compose logs service_name